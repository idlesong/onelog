<h1>Listing users</h1>

<ul class="list-group">
  <% @users.each do |user| %>
  <li class="list-group-item">
    <%= image_tag("blog2-thumb.jpg") %> <%= link_to user.name, user %>
  </li>
  <% end %>
</ul>
